spring:
  application:
    name: auth-server
  profiles:
    active: default

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://127.0.0.1:8761/eureka/}

server:
  port: 9000
