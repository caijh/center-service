spring:
  application:
    name: auth-server
  profiles:
    active: default
  cloud:
    config:
      enabled: true
      discovery:
        enabled: false
      uri: http://192.168.33.10:30080/config-server

